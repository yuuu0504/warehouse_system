# .env.example

# 模式切換: production (Postgres) | development (SQLite)
# 如果是 development，會忽略 DATABASE_URL，直接使用 local sqlite 檔案
APP_ENV=development

# PostgreSQL 連線設定 (Docker 預設)
# 格式: postgresql+asyncpg://user:password@host:port/dbname
DATABASE_URL=postgresql+asyncpg://wms_user:wms_password@localhost:5432/wms_db

# SQLite 設定 (若 APP_ENV=development)
SQLITE_URL=sqlite+aiosqlite:///./local_dev.db